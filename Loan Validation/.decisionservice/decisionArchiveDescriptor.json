{
  "contentVersion" : "1.0.0",
  "buildTime" : "${buildTime}",
  "mavenGroupId" : "${project.parent.groupId}",
  "mavenArtifactId" : "${project.parent.artifactId}",
  "mavenVersion" : "${project.parent.version}",
  "decisionServiceName" : "${project.parent.groupId}",
  "decisionServiceId" : "${project.parent.artifactId}",
  "decisionServiceVersion" : "${project.parent.version}",
  "engineRuntimeVersion" : "${engine-compact-runtime.version}",
  "engineAPIVersion" : "${engine-api.version}",
  "decisionId" : "${decisionId}",
  "commitId" : "${commitId}",
  "mlIntegrationRuntimeVersion" : "${ml-integration-runtime.version}"
}